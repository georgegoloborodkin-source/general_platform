# ── Company Second Brain V2 — Dependencies ──

# Web framework & server
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
uvloop>=0.21.0;sys_platform!='win32'
httptools>=0.6.0

# Faster JSON serialisation (20-50% vs stdlib)
orjson>=3.10.0

# Data validation
pydantic>=2.9.0

# HTTP client
httpx>=0.27.0

# File uploads
python-multipart>=0.0.9

# ── Anthropic SDK (Claude-native stack) ──
anthropic>=0.40.0

# ── JWT auth ──
PyJWT>=2.9.0

# ── PDF extraction (fallback after Claude-native) ──
PyPDF2>=3.0.0
pdfplumber>=0.10.0
PyMuPDF>=1.24.0

# ── Office file parsing ──
openpyxl>=3.1.0
xlrd>=2.0.1

# ── Supabase (backend retrieval for Agentic RAG) ──
supabase>=2.0.0

# ── Optional: legacy local model support ──
ollama>=0.3.0
openai>=1.0.0